/* const menuContenido=document.getElementById("id-prueba")
const elementos = menuContenido.querySelectorAll(".contenedorUrlfuncinalidad a p") */

//--------funciones

/* function cambiar_nombres(){
    console.log(menuContenido)
    for(elemento of elementos){
        console.log(elemento.textContent)
        elemento.textContent=elemento.textContent.replace("_"," ")
    }
} */

//--------------Eventos delegación.

/* menuContenido.addEventListener("cilck",e=>{
    let elementoActual=e.target
    if(elementoActual.matches("p")){
        console.log(elementoActual.textContent)
    }
}) */

//---------------Precarga

/* cambiar_nombres() */